/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/cvtwocolumns',
    assetPrefix: '/cvtwocolumns/',
    images: {
        unoptimized: true
    }
}

module.exports = nextConfig
